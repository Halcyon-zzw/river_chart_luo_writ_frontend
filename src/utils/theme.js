// 主题颜色配置
export const theme = {
  // 背景色
  bgPrimary: '#f5f5f5', // 主背景色
  bgSecondary: '#ffffff', // 次级背景色
  bgCard: '#ffffff', // 卡片背景色
  bgOverlay: 'rgba(0, 0, 0, 0.05)', // 遮罩背景

  // 文字颜色
  textPrimary: '#333333', // 主要文字
  textSecondary: '#666666', // 次要文字
  textTertiary: '#999999', // 三级文字
  textPlaceholder: '#cccccc', // 占位文字

  // 导航栏
  navbarBg: 'rgba(255, 255, 255, 0.95)', // 导航栏背景
  navbarText: '#333333', // 导航栏文字
  navbarBorder: 'rgba(0, 0, 0, 0.1)', // 导航栏边框

  // 卡片
  cardBg: '#ffffff', // 卡片背景
  cardShadow: 'rgba(0, 0, 0, 0.08)', // 卡片阴影
  cardBorder: 'rgba(0, 0, 0, 0.05)', // 卡片边框

  // 按钮
  btnPrimary: '#00c4b3', // 主按钮
  btnSecondary: 'rgba(0, 196, 179, 0.1)', // 次要按钮背景
  btnText: '#ffffff', // 按钮文字

  // 标签
  tagBg: 'rgba(0, 196, 179, 0.1)', // 标签背景
  tagText: '#00c4b3', // 标签文字

  // 分割线
  divider: 'rgba(0, 0, 0, 0.08)', // 分割线

  // 渐变遮罩（用于图片上的文字）
  gradientOverlay: 'linear-gradient(180deg, transparent 0%, rgba(0, 0, 0, 0.7) 100%)',

  // 菜单
  menuBg: '#ffffff', // 菜单背景
  menuItemBg: 'rgba(0, 0, 0, 0.03)', // 菜单项背景
  menuItemActiveBg: 'rgba(0, 0, 0, 0.08)', // 菜单项激活背景
}

export default theme
