📋 未实现/待完善的功能清单

  1. ⚠️ 核心组件缺失

  🔴 标签选择器组件（高优先级）

  位置: src/components/TagSelector/
  用途:
  - 创建图片时选择标签
  - 创建文本时选择标签
  - 支持创建新标签

  当前状态:
  - ❌ 组件未创建
  - 各页面中用占位提示代替：uni.showToast({ title: '标签选择功能开发中' 
  })

  影响页面:
  - create-image.vue (line ~220)
  - create-note.vue (line ~184)

  ---
  🟡 子分类选择器组件（中优先级）

  位置: src/components/SubCategorySelector/
  用途:
  - 创建内容时选择子分类
  - 级联显示主分类->子分类

  当前状态:
  - ❌ 组件未创建
  - 各页面中用占位提示代替

  影响页面:
  - create-image.vue (line ~213)
  - create-note.vue (line ~177)

  ---
  2. ⚠️ 图片处理功能未集成

  🟡 图片裁剪功能

  依赖: lime-painter (已安装)
  功能:
  - 上传图片后支持裁剪
  - 调整图片尺寸
  - 应用滤镜（可选）

  当前状态:
  - ✅ 依赖已安装
  - ❌ 未在 create-image.vue 中集成

  ---
  3. ⚠️ 空状态图片缺失

  需要添加以下图片：
  - ❌ src/static/images/empty-recent.png (最近页空状态)
  - ❌ src/static/images/empty-collection.png (收藏页空状态)

  ---
  4. ⚠️ 后端接口适配问题

  🔴 需要确认的接口细节

  a) 用户登录接口 (/user/login)
  - 问题: 不确定接口需要什么参数
  - 当前实现: 传递 { deviceId: 'xxx' }
  - 建议: 与后端确认具体参数格式

  b) 图片上传接口 (/content/upload-images)
  - 问题: 批量上传的具体实现方式
  - 当前实现:
  // create-image.vue 中逐个上传
  uni.uploadFile({
    url: config.API_BASE_URL + '/content/upload-images',
    filePath: img.url,
    name: 'file'
  })
  - 建议: 确认是单图上传还是真的批量上传

  c) 数据返回格式
  - 问题: 不确定后端统一返回格式
  - 当前兼容处理:
  const list = res.data?.records || res.data?.list || res.data || []
  - 建议: 统一后端返回格式，简化前端处理

  ---
  5. ⚠️ 功能细节待完善

  🟢 已占位但未实现的功能

  1. 分类创建功能 (browse.vue line ~163)
    - 点击浮动按钮的"新建分类"选项
  2. 设置页面 (profile.vue line ~77)
    - 我的页面中的"设置"入口
  3. 分享功能 (多个详情页)
    - 图片详情页分享
    - 文本详情页分享
  4. 权限引导 (需求文档提到)
    - 相机/相册权限请求
    - 权限被拒绝时的引导提示

  ---
  🎯 优先级建议

  🔴 必须完成（影响核心流程）

  1. 标签选择器组件 - 否则无法完整创建内容
  2. 子分类选择器组件 - 否则无法关联分类
  3. 后端接口对接确认 - 确保数据正常交互

  🟡 建议完成（提升体验）

  4. 图片裁剪功能 - 提升图片上传体验
  5. 空状态图片 - 优化视觉体验
  6. 权限引导 - 避免用户困惑

  🟢 可延后（锦上添花）

  7. 分类创建功能 - 可以先通过后台管理
  8. 设置页面 - 当前功能已够用
  9. 分享功能 - 非核心需求

  ---
  📝 下一步行动建议

  你希望我优先实现哪个功能？我建议按照以下顺序：

  1. 先实现标签选择器组件 - 最关键
  2. 然后实现子分类选择器 - 完善创建流程
  3. 最后优化图片处理和视觉效果