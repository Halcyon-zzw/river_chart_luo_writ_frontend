
-----------分界线，上述需求已经处理，请忽略------------------
1. 格式调整
- 删除浏览页面顶部浏览标题，但整体布局不变；
- 最近页面顶部最近下移，按照收藏页面顶部收藏格式及页面位置。
- 图片内容、文本内容编辑、创建页面整体风格、配置应该一致，请使用主流实现方式优化；如统一并优化底部吸底方式和格式。


2. 图片列表页逻辑调整
- 点击图片标题进入详情页；
- 标题下方需要展示标签、日期，进入图片详情同样需要展示标签、日期
- 进入图片详情滑动屏幕返回时应该退到图片详情页；


  我的疑问：

  关于需求 1.1：
  1. "浏览页面"是指主分类列表页（browse.vue）吗？ y
  2. 删除"浏览"标题后，是否保留自定义导航栏组件？还是只删除标题文字？只删除文字；
  3. 补充：收藏页面，不需要展示导航栏的“返回”

  关于需求 1.2：
  1. 能否提供收藏页面的截图或具体的标题位置参数？请自己参考代码；
  2. "下移"是指增加 top padding 还是调整其他样式？不知道这些名词，效果就是“最近”字体、位置同收藏页面的“收藏”

  关于需求 2.1：
  1. 当前图片列表页的交互是怎样的？点击整个卡片还是点击图片？点击标题进入图片编辑页，点击图片进入图片详情页。
  2. 改为点击标题后，点击图片本身应该有什么行为？预览图片？对，预览图片；

  关于需求 2.3：
  1. "进入图片详情滑动屏幕返回时应该退到图片详情页" - 这个描述似乎有循环？ 进入图片详情滑动屏幕返回时应该退到图片列表页；
    - 是否应该是："从图片预览滑动返回时，应该回到图片详情页"？
    - 还是指图片详情页内有多张图片，滑动切换时的逻辑？

-----------分界线，上述需求已经处理，请忽略------------------
1. 图片创建、编辑页，中部可滑动容器滑动卡卡的，改为与文本类型一样的容器配置；
2. 图片列表页，点击内容标题字体进入图片详情页；点击图片进入图片预览页；日期始终靠右对齐；标签默认展示3个，其他折叠。
3. 图片编辑页，子分类默认展开；
4. 进入文本内容编辑页，不需要请求接口：/content/1230/tags

疑问：
  关于需求 2：
  1. 交互变更确认：新需求中"点击标题进入图片详情页"，那么如何进入图片编辑页？
    - 是否需要在详情页添加编辑按钮？已经存在
    - 还是通过其他方式（如长按、滑动菜单）进入编辑？
  2. 预览页 vs 详情页：
    - "点击图片进入图片预览页" - 这是指使用图片预览组件（ImagePreview）的弹窗模式吗？
    - 还是跳转到另一个专门的预览页面？预览页面，已经存在的页面；
  3. 标签折叠逻辑：
    - 标签折叠后，是否需要显示"更多"按钮或"+N"提示？
    - 展开后是否可以再次折叠？
    参考content-note.vue的标签折叠逻辑；
-----------分界线，上述需求已经处理，请忽略------------------
1. 图片预览页页面通过左滑屏幕返回时，应该返回图片列表页；（点击导航栏返回逻辑正常）
2. 查看/Users/zhuzhiwei/learn/river-chart-luo-writ-frontend/river-chart-luo-writ-frontend/src/demand/Clipboard_Screenshot_1768463684.png，图片编辑页，滑动到底时，再次上滑空白区域不会消失，请修复；考虑文本编辑页是否有该问题；

-----------分界线，上述需求已经处理，请忽略------------------
图片编辑页， 底部标签内容被按钮遮挡；
进入图片编辑页，不需要请求/content/1227/tags；

很好，请将文本编辑页按图片编辑页进行调整；
  
-----------分界线，上述需求已经处理，请忽略------------------
1、收藏列表页单个元素展示的信息同内容列表页一样展示信息；如标签、日期等；
2、浏览历史单个元素需要展示标签、展示“浏览时间: 2026-01-16 10:12:12”；
3、最近页面中查询浏览时间选项通过后端接口获取值；请查看后端接口；/comment/listTimeRangeTypeList
4、进入最近页面需要刷新
5、收藏页面滑动问题，滑动到底部后没东西；收藏列表的容器按照最近页面修改。


  我的疑问

  关于需求3（时间筛选API）：

  1. ❓ 接口路径 /comment/listTimeRangeTypeList 看起来前缀是 /comment/，但这个模块似乎应该与浏览历史相关，是否应该是 /browse-history/listTimeRangeTypeList？
  /browse-history/listTimeRangeTypeList, 返回数据请查看最新接口文档。
  2. ❓ 这个接口的返回数据格式是什么？是否类似：
  {
    "code": 200,
    "data": [
      { "value": "all", "label": "全部" },
      { "value": "today", "label": "今天" }
    ]
  }

  关于需求2（浏览历史展示）：

  3. ❓ "浏览时间: 2026-01-16 10:12:12" 这个格式是否需要显示为完整的带"浏览时间:"前缀的格式？还是保持现在只显示时间的方式？需要前缀

  关于需求5（收藏页面滑动）：

  4. ❓ "滑动到底部后没东西" 具体表现是什么？是没有显示"已加载全部"提示？还是底部空白太多？底部空白太多，按照最近页面的页面容器设置优化

-----------分界线，上述需求已经处理，请忽略------------------
浏览历史页：
1、分页查询入参有误，出参浏览时间字段有误；时间列表接口结果有误，请查看最新接口修改；
2、收藏列表交互逻辑同内容列表页一致；如点击图片内容标题应该进入内容详情页。


关于需求1（浏览历史接口问题）：

  1. ❓ 分页查询入参有误 - 具体是哪些参数有问题？
    - 当前我在 recent.vue 中使用的参数：
    {
    contentType: 'image' | 'note',
    pageNum: 1,
    pageSize: 20,
    title: '搜索关键词',  // 搜索参数
    timeRange: 'all'     // 时间范围
  }
    - 正确的参数格式应该是什么？
  2. ❓ 出参浏览时间字段有误 -
    - 当前我使用的字段：item.browseTime
    - 正确的字段名应该是什么？
  3. ❓ 时间列表接口结果有误 -
    - /browse-history/listTimeRangeTypeList 接口返回的数据格式是什么？
    - 我当前期望的格式是：[{ value: 'all', label: '全部' }]

上述问题都可以通过查询最新接口文档获取，请告诉我你理解的最新接口地址。

  关于需求2（收藏列表交互）：

  4. ❓ 确认交互逻辑：
    - 图片内容：点击标题 → 详情页，点击图片 → 预览？ y
    - 文本内容：点击标题 → 详情页，点击卡片 → 详情页？ y
  
-----------分界线，上述需求已经处理，请忽略------------------
最近页面图片列表中，点击标题 → 详情页，点击图片 → 预览
所有图片预览页，滑动到最后一张时不应该立马弹出“已是最后一张了”，应该在最后一张时，再次滑动到下一张时提示；


# 说明
1. 已完成的需求追加到demand_tree_completed.md中，不需要修改本文件.
2. 忽略最后出现的“-----------分界线，上述需求已经处理，请忽略------------------”字样以上的需求
3. 请先给出你的理解，有问题提出来。