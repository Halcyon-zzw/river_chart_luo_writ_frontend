
  1. 后端 API 接口细节

  需要确认 Swagger 文档中的接口：
  - 文件上传接口：路径和参数格式？
  http://localhost:8080/swagger-ui/index.html中“数据内容表管理”下的“批量上传图片”
  - 内容查询接口：是否支持分页、按分类筛选？返回的图片字段是URL还是base64？
  url
  - 用户ID获取：既然是多用户平台但本期不实现登录，那用户ID如何获取？是硬编码、本
  地生成、还是首次启动时注册匿名用户？
  暂时不考虑用户注册，通过/user/login获取用户id

  2. 数据模型关键字段

  根据 Swagger，我看到了这些表，但需要确认：
  - Contents 表中的 content_type
  字段值：图片类型和文本类型分别是什么？（如：1=图片，2=文本）
  图片：image, 文本: note
  - Contents 表中存储图片的字段名？存储富文本的字段名？
  imageUrl
  - 子分类关联主分类的字段名？
  noteContent

  3. 标签功能本期实施范围

  你提到"标签本期没有实现"，但又说"支持按标签筛选"，具体是：
  指通过标签搜索content暂时不实现。但是关联标签还是要实现，同时支持创建新的标签，关联过程中选择已经存在的标签。


  4. 富文本编辑器技术方案

  对于支持 H5、小程序、App 三端的富文本编辑器：
  - 方案A：使用 uni-app 内置的 <editor> 组件（仅支持 App 和 H5，小程序支持有限）
  - 方案B：使用第三方库如 mp-html（展示）+ 自定义工具栏（编辑）
  - 方案C：H5 用成熟编辑器（quill/tinymce），小程序用简化版

  你倾向哪种方案？或者本期先做简化版（只支持基础格式），后续迭代增强？
  先用方案B看看效果。

  5. 图片处理细节

  - 多图上传：创建图片内容时，一次可以上传多张图片生成多条内容记录，还是一张图片
  一条记录？
  多条内容，imageUrl存储返回的url
  - 图片裁剪/滤镜：uni-app
  默认的图片选择器没有这些功能，是否需要集成第三方插件（如
  luch-cropper），还是本期先不做？
  集成第三方插件

  6. Tab 栏最终顺序确认

  根据回答，Tab 顺序应该是：
  1. 最近（本期仅展示友好提示）
  2. 浏览（启动默认页）
  3. 收藏
  4. 我的

  是否正确？
  是

  7. 项目初始化用户方案

  既然本期不做登录但是多用户平台，建议：
  - 方案A：首次启动时生成随机 UUID 作为用户ID，存储在本地
  - 方案B：在代码中硬编码一个测试用户ID（如：userId = 1）
  - 方案C：App 启动时调用后端创建匿名用户接口

  你选择哪种？
  方案C，通过/user/login获取用户id