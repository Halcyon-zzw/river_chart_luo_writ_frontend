
-----------分界线，上述需求已经处理，请忽略------------------
1、卡片右滑显示编辑功能太灵敏，下滑过程中容易误触；横向滑动超过 20rpx 且横向位移大于纵向位移 1.5
  倍时才算有效滑动；
2、所有长按松手才进入批量删除的地方改为长按2s直接进入批量删除操作；长按 2 秒进入批量删除时，卡片轻微缩放
3、所有右滑返回逻辑同导航栏返回保持逻辑一致；如编辑过程中右滑屏幕返回时不会提示用户保存信息；请检查所有类似返回的地方；导航返回实现了提醒功能；在屏幕左边缘右滑返回的时候没有提示；
4、新增子分类中，如果选择了其他主分类，创建成功后跳转页面上方的标题应该为所选主分类名称。当前错误显示其他主分类名称。创建子分类成功后，应该跳转到新选择的主分类的子分类列表页
5、主分类、子分类编辑页，压缩描述、封面图的空间，所有内容在一个页面能展示，保存按钮始终在最底部（固定在最底部，滚动过程始终保存着最底部）。编辑页上下滑动不够丝滑（改用 page-meta + scroll-view 优化），检查其他编辑页是否有同样的问题。
描述框改为 min-height: 120rpx，封面图预览改为300rpx * 300rpx;
6、进入文本内容编辑页，加租、斜体文字组件不直接显示。点击内容编辑框后显示在键盘上方。内容编辑框失焦后，隐藏组建。点击简介编辑框不显示组件。
注意：文本类型编辑、创建页面，点击内容编辑框、简介编辑框，会跳出手机键盘，“取消”、“发布”按钮不需要调整到键盘上方；
需要将工具栏拆分为两部分：
    - 格式化工具栏（加粗、斜体等）- 跟随键盘
    - 操作按钮（取消、发布）固定在最底部，滚动过程始终保存着最底部
7、内容新增按钮同分类新增按钮保持在一个水平位置；
8、主分类滑动到最底部后，再次上滑到顶部时，卡片会被搜索框遮挡。子分类卡片列表页，下滑后，搜索框应该固定在顶部，并且子分类列表页下滑后，再次上滑滑不动。

疑问：
 1. 需求3 - "右滑返回逻辑"：这里的"右滑"指的是屏幕左边缘向右滑动的系统手势返回吗？是的；
 uni-app小程序中这个手势是否可以拦截？小程序不支持就忽略，但需要支持app场景；
  2. 需求6 - "加租、斜体"是否应该是"加粗、斜体"？是的
  3. 需求8 - 子分类页"下滑后再次上滑滑不动"，具体是什么情况？是页面完全无法滚动，还是滚动不流畅？
  无法继续向上滑动查看更上面的内容，像是现实重叠了。

  需求8：滚动和遮挡问题

  问题A（主分类）：滑到底部再上滑，卡片被搜索框遮挡
  问题B（子分类）：下滑后搜索框应固定顶部，但上滑后滑不动（卡住）

  我的疑问：
  1. 主分类的遮挡问题 - 之前已经设置了 z-index: 1000，还是有遮挡吗？还有遮挡，滑动到底部后，整个页面似乎会重新加载，再次上滑到顶的时候，第一张卡片就在搜索框及”共几条记录“描述的后方；需要调整回来，不能被遮挡。
  2. 子分类"滑不动"具体指什么情况？是scroll-view卡住了？还是其他问题？
  也是遮挡问题，第一张卡片甚至被导航栏遮挡。并且滑动过程中，搜索框应该始终在顶部。

  需要确认的问题：

  1. 需求3（右滑返回）：微信小程序右滑返回是系统行为，onBackPress 无法拦截。可能的方案：暂时不处理；
    - 方案A：在页面配置中禁用右滑返回 disableSwipeBack: true，但这会影响用户体验
    - 方案B：只能提示用户使用导航栏返回按钮

  您希望采用哪种方案？
  2. 需求5（封面图尺寸）：从 150rpx 改为 300rpx，是否所有编辑页都要改？包括 create-image.vue 吗？
  所有编辑页都调整。不包括 create-image.vue
  3. 需求8（滚动问题）：需要更详细的描述来定位问题，或者我可以先检查代码？
先检查代码。

新增优化：
1、文本内容创建、编辑页，底部的“取消、发布“始终在最底部，点击任何输入框都不应该跳转到键盘上方。

# 说明
1. 已完成的需求追加到demand_tree_completed.md中，不需要修改本文件.
2. 忽略最后出现的“-----------分界线，上述需求已经处理，请忽略------------------”字样以上的需求
3. 请先给出你的理解，有问题提出来。