-----------分界线，上述需求已经处理，请忽略------------------

1. 统一维护版本号，替换所有使用到的地方。
2. 根据后端接口，实现最近浏览功能。

疑问：
  关于版本号：

  1. 统一版本号应该是多少？ 是使用 1.0.0 还是其他版本号？
  使用1.0.0即可；
  2. package.json 需要同步吗？ 目前是 0.0.0，是否需要改为与 manifest.json 一致？
  改成一致；
  3. manifest.json 需要自动同步吗？ 还是只在 config.js 中维护即可？
  自动同步；

  关于最近浏览功能：

  1. 浏览历史记录什么内容？
    - 只记录内容详情页（图片、笔记）的浏览？
    - 还是包括主分类、子分类的访问？
    只记录内容列表页信息，页面与内容列表页类似。顶部有类型选择的tab；
  2. 使用哪个后端接口？
    - 查看 API 文档，有 operation-log 相关接口
    /browse-history相关接口
    - 需要确认：创建日志的接口是 POST /operation-log/create 吗？
    不是，/browse-history下有接口
    - 查询最近浏览是否有专门的接口，还是通过 operation-log 的分页查询？
    /browse-history下有分页查询
  3. 何时记录浏览？
    - 进入详情页时自动记录？
    自动记录
    - 还是需要停留一定时间才记录？
  4. 最近浏览的UI展示：
    - 展示哪些信息？（缩略图、标题、时间、类型）
    文本：标题、最近浏览时间。
    图片：展示缩略图。
    - 列表还是卡片？
    列表
    - 是否分组（今天、昨天、更早）？
    不用，需要搜索框，用户id，今天、三天内、七天内，一个月内。可作为时间选择框条件供用户选择。
    - 是否支持删除单条或清空全部？
    支持。
-----------分界线，上述需求已经处理，请忽略------------------

1、进入系统调用/login获取用户信息，并将userInfo设置为全局信息；
2、所有接口的请求头里面新增userId字段（x-user-id:${userInfo.userId}）。

疑问：

  关于登录接口：

  1. /login 接口需要传什么参数？
    - 是否需要微信授权登录（openid、code等）？
    - 还是自动登录（不需要参数）？
    暂时不传参数
  2. 接口返回的数据格式是什么？
  看后端接口
  // 例如：
  {
    "code": 200,
    "data": {
      "userId": 123,
      "username": "xxx",
      "nickname": "xxx",
      "avatar": "xxx",
      ...
    }
  }
  3. 如果登录失败，应该如何处理？
    - 显示错误提示？
    - 跳转到登录页？
    跳转到登录页

  关于请求头：

  1. x-user-id 的值应该是什么？
    - 是 userInfo.userId？
    - 还是 userInfo.id？
    前者

-----------分界线，上述需求已经处理，请忽略------------------

1、进入系统调用/login获取用户信息，并将userInfo设置为全局信息；
2、所有接口的请求头里面新增token字段（token:${userInfo.token}）。删除原来请求头x-user-id字段

  1. 请确认后端 /login 接口返回的数据中是否包含 token 字段？ 查看后端接口
  2. Token 存储位置：
    - 应该存储在 userInfo 中（userInfo.token）？y
    - 还是存储在单独的位置？
  3. 请求头格式确认：
    - 是 token: ${userInfo.token} 这样的格式？
    - 还是 Authorization: Bearer ${token} 这样的格式？后者

-----------分界线，上述需求已经处理，请忽略------------------
浏览历史需要展示内容信息，如文本类型的标题，描述，图片类型的图片；
数据从data.rows.contentDTO获取。

# 说明
1. 已完成的需求追加到demand_tree_completed.md中，不需要修改本文件.
2. 忽略最后出现的“-----------分界线，上述需求已经处理，请忽略------------------”字样以上的需求
3. 请先给出你的理解，有问题提出来。